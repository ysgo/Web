<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="resource.MemberMapper">
	<!-- 회원 가입 -->
<insert id="signup">
	insert into tbl_member(userId, userPass, userName, userPhon)
		values(#{userId}, #{userPass}, #{userName}, #{userPhon})  
</insert>

<!-- 로그인 -->
<select id="signin" resultType="vo.MemberVO">
	select
	    userId, userName, userPass, userPhon, userAddr1, userAddr2, userAddr3, regiDate, verify
	from tbl_member
	where userId = #{userId}
</select>

 	<!-- <select id="selectMember"  resultType="vo.MemberVO">
    	select id, writer, title, content, to_char(writedate,'yyyy"년"mm"월"dd"일" HH24"시"mi"분"') writedate, cnt from tbl_board
 	</select>
 	<select id="selectMemberOne"  parameterType="_int" resultType="vo.MemberVO">
      	select * from tbl_board where id=#{id}
 	</select>
  	<insert id="insertMember"  parameterType="vo.MemberVO">
  		<selectKey resultType="int" keyProperty="id" order="BEFORE">
      		select board_seq.nextval from dual     
    	</selectKey>  
    		insert into tbl_board values(board_seq.nextval, #{writer}, #{title}, #{content}, sysdate, #{cnt})
	</insert>	
	<delete id="deleteMember"  parameterType="_int"  >
    	delete from tbl_board where id = #{id}
	</delete>
	<update id="updateMember"  parameterType="vo.MemberVO"  >
    	update tbl_board set  
				writer = #{writer}, 
				title = #{title},
				content = #{content},
				writedate = sysdate,
				cnt = cnt+1
		where id = #{id}
	</update> -->
</mapper>